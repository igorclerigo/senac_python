# A entrada de dados é realizada através do comando input
# IF ELSE e ELIF são comandos para trabalhar com condições, isto é, testar condições
# O else será usado quando a condiçao 1 não for respeitada. Quando isso acontecer, o código depois do else será executado.

nome = input ("entre com o seu nome: ")
sobrenome = input ("entre com o seu sobrenome: ")

nomecompleto = nome + " " + sobrenome

print(nomecompleto)

# O resultado está sendo mostrado através da variável nomecompleto, que é responsável por guardar na memória o valor da soma entre as variáveis
# nome e sobrenome (com um espaço no meio). O valor destas é recebido através do input.


